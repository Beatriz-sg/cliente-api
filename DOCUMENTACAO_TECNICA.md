# 📖 DOCUMENTAÇÃO TÉCNICA - FAVORITOS\n\n## Para Novos Desenvolvedores\n\n### Arquitetura do Sistema de Favoritos\n\n```\n┌─────────────────────────────────────────────────────┐\n│          USER INTERFACE COMPONENTS                  │\n├─────────────────────────────────────────────────────┤\n│                                                     │\n│  ├─ TopRatedStores.tsx     [Favoritos de lojas]   │\n│  ├─ NearbyStores.tsx       [Favoritos de lojas]   │\n│  ├─ AllStores.tsx          [Favoritos de lojas]   │\n│  ├─ OffersCarousel.tsx     [Favoritos de produtos]│\n│  ├─ produto.tsx            [Detalhe + favorito]   │\n│  └─ favoritos.tsx          [Listagem]             │\n│                                                     │\n└──────────────┬──────────────────────────────────────┘\n               │\n               ▼\n┌─────────────────────────────────────────────────────┐\n│        SERVICE LAYER                                │\n├─────────────────────────────────────────────────────┤\n│                                                     │\n│  favoritosService.ts                               │\n│  ├─ getLojasFavoritas()      [GET]                │\n│  ├─ getProdutosFavoritos()   [GET]                │\n│  ├─ adicionarLojaFavorita()  [POST]               │\n│  ├─ adicionarProdutoFavorito() [POST]             │\n│  ├─ removerLojaFavorita()    [DELETE]             │\n│  ├─ removerProdutoFavorito() [DELETE]             │\n│  ├─ isLojaFavorita()         [CHECK]              │\n│  └─ isProdutoFavorito()      [CHECK]              │\n│                                                     │\n└──────────────┬──────────────────────────────────────┘\n               │\n               ▼\n┌─────────────────────────────────────────────────────┐\n│          BACKEND API                                │\n├─────────────────────────────────────────────────────┤\n│                                                     │\n│  GET  /api/favoritos/lojas                         │\n│  GET  /api/favoritos/produtos                      │\n│  POST /api/favoritos/lojas/{id}                    │\n│  POST /api/favoritos/produtos/{id}                 │\n│  DELETE /api/favoritos/lojas/{id}                  │\n│  DELETE /api/favoritos/produtos/{id}               │\n│                                                     │\n└──────────────┬──────────────────────────────────────┘\n               │\n               ▼\n┌─────────────────────────────────────────────────────┐\n│       DATABASE (Persistência)                       │\n├─────────────────────────────────────────────────────┤\n│                                                     │\n│  TABLE: favoritos_lojas                            │\n│    └─ usuario_id, loja_id                          │\n│                                                     │\n│  TABLE: favoritos_produtos                         │\n│    └─ usuario_id, produto_id                       │\n│                                                     │\n└─────────────────────────────────────────────────────┘\n```\n\n---\n\n## Padrões de Implementação\n\n### 1. Estado com Set (Performance)\n```typescript\n// ✅ CORRETO - O(1) lookup\nconst [favoritos, setFavoritos] = useState<Set<number>>(new Set());\nif (favoritos.has(id)) { ... }\n\n// ❌ ERRADO - O(n) lookup\nconst [favoritos, setFavoritos] = useState<number[]>([]);\nif (favoritos.includes(id)) { ... }\n```\n\n### 2. Autenticação\n```typescript\n// Sempre verificar se usuário está autenticado\nconst token = await getToken();\nconst [logado, setLogado] = useState(false);\n\nasync function toggleFavorito(id: number) {\n  if (!logado) return; // Proteção\n  // ... resto da lógica\n}\n```\n\n### 3. Carregamento de Favoritos\n```typescript\n// Always load on component mount\nuseEffect(() => {\n  verificarAutenticacao();\n  carregarFavoritos();\n}, []);\n\nasync function carregarFavoritos() {\n  try {\n    const lista = await getLojasFavoritas();\n    setFavoritos(new Set(lista.map((l) => l.id)));\n  } catch {\n    // Silenciosamente falha se não autenticado\n  }\n}\n```\n\n### 4. Toggle com Persistência\n```typescript\nasync function toggleFavorito(id: number) {\n  if (!logado) return;\n  const novoSet = new Set(favoritos);\n  \n  if (novoSet.has(id)) {\n    // Remover\n    novoSet.delete(id);\n    setFavoritos(novoSet);\n    await removerLojaFavorita(id).catch(() => {});\n  } else {\n    // Adicionar\n    novoSet.add(id);\n    setFavoritos(novoSet);\n    await adicionarLojaFavorita(id).catch(() => {});\n  }\n}\n```\n\n### 5. Deduplicação\n```typescript\n// Sempre deduplicar dados do servidor\nconst seen = new Set<number>();\nconst unique = data.filter((item: any) => {\n  if (seen.has(item.id)) return false;\n  seen.add(item.id);\n  return true;\n});\n```\n\n---\n\n## Guia de Manutenção\n\n### Adicionar Novo Componente com Favoritos\n\n**1. Importe o serviço:**\n```typescript\nimport {\n  getLojasFavoritas,\n  adicionarLojaFavorita,\n  removerLojaFavorita,\n} from \"../../services/favoritosService\";\nimport { getToken } from \"../../services/authService\";\n```\n\n**2. Adicione estado:**\n```typescript\nconst [favoritos, setFavoritos] = useState<Set<number>>(new Set());\nconst [logado, setLogado] = useState(false);\n```\n\n**3. Carregue favoritos:**\n```typescript\nuseEffect(() => {\n  (async () => {\n    const token = await getToken();\n    setLogado(!!token);\n    \n    if (token) {\n      try {\n        const lista = await getLojasFavoritas();\n        setFavoritos(new Set(lista.map((l) => l.id)));\n      } catch { }\n    }\n  })();\n}, []);\n```\n\n**4. Implemente toggle:**\n```typescript\nasync function toggleFavorito(id: number) {\n  if (!logado) return;\n  const novoSet = new Set(favoritos);\n  if (novoSet.has(id)) {\n    novoSet.delete(id);\n    setFavoritos(novoSet);\n    await removerLojaFavorita(id).catch(() => {});\n  } else {\n    novoSet.add(id);\n    setFavoritos(novoSet);\n    await adicionarLojaFavorita(id).catch(() => {});\n  }\n}\n```\n\n**5. Renderize UI:**\n```typescript\n<TouchableOpacity onPress={() => toggleFavorito(item.id)}>\n  <MaterialIcons\n    name={favoritos.has(item.id) ? \"favorite\" : \"favorite-border\"}\n    size={24}\n    color=\"#ff4d8d\"\n  />\n</TouchableOpacity>\n```\n\n---\n\n## Tratamento de Erros\n\n### Erro: \"Cannot read property 'has' of undefined\"\n**Causa**: Estado não inicializado corretamente\n**Solução**: `useState<Set<number>>(new Set())` ao invés de `useState()`\n\n### Erro: \"adicionarLojaFavorita is not a function\"\n**Causa**: Import faltando\n**Solução**: Verificar imports em `favoritosService.ts`\n\n### Problema: Favoritos desaparecem ao recarregar\n**Causa**: Não carrega favoritos ao montar\n**Solução**: Adicionar useEffect com `carregarFavoritos()`\n\n### Problema: Duplicação de lojas\n**Causa**: Deduplicação não implementada\n**Solução**: Usar `seen.has(id)` antes de adicionar\n\n### Problema: Coração não muda visualmente\n**Causa**: Usando `Array.includes()` ao invés de `Set.has()`\n**Solução**: Converter para Set<number>\n\n---\n\n## Testing\n\n### Teste Unitário\n```typescript\nimport { toggleFavorito } from \"./component\";\n\ntest(\"deve adicionar favorito\", async () => {\n  const { result } = renderHook(() => useFavoritos());\n  \n  await act(async () => {\n    await result.current.toggleFavorito(123);\n  });\n  \n  expect(result.current.favoritos.has(123)).toBe(true);\n});\n```\n\n### Teste de Integração\n```typescript\ntest(\"deve persistir favorito após reload\", async () => {\n  const { getByTestId, rerender } = render(<Component />);\n  \n  // Favoritar\n  fireEvent.press(getByTestId(\"heart-btn\"));\n  await waitFor(() => expect(getByTestId(\"heart\")).toHaveColor(\"#ff4d8d\"));\n  \n  // Simular reload\n  rerender(<Component />);\n  \n  // Verificar persistência\n  expect(getByTestId(\"heart\")).toHaveColor(\"#ff4d8d\");\n});\n```\n\n---\n\n## Performance\n\n### Otimizações Implementadas\n1. **Set ao invés de Array** - O(1) lookup vs O(n)\n2. **Deduplicação na source** - Evita duplicatas\n3. **Lazy loading** - Carrega apenas ao montar\n4. **Error silencing** - Não bloqueia com erros de rede\n\n---\n\n## Roadmap Futuro\n\n- [ ] Cache local (AsyncStorage)\n- [ ] Sincronização em background\n- [ ] Notificações de novos favoritos\n- [ ] Compartilhamento de favoritos\n- [ ] Ordenação de favoritos\n- [ ] Filtros de favoritos\n- [ ] Exportação de favoritos\n\n---\n\n## Referência Rápida de APIs\n\n```typescript\n// Lojas\nawait getLojasFavoritas() // GET\nawait adicionarLojaFavorita(id) // POST\nawait removerLojaFavorita(id) // DELETE\nawait isLojaFavorita(id) // CHECK\n\n// Produtos\nawait getProdutosFavoritos() // GET\nawait adicionarProdutoFavorito(id) // POST\nawait removerProdutoFavorito(id) // DELETE\nawait isProdutoFavorito(id) // CHECK\n```\n\n---\n\n## Contato\n\nSe encontrar problemas ou tiver dúvidas:\n1. Verifique TROUBLESHOOTING em INSTRUCOES_VALIDACAO.md\n2. Revise os padrões acima\n3. Consulte o RELATORIO_FINAL.md\n4. Abra uma issue no repositório\n\n---\n\n**Última atualização**: 16/06/2026\n**Status**: ✅ Documentação Completa\n"